var _ = require('underscore');

/**
 * @param {array} options.SOMETHING Definition of the param
 * @return {!webdriver.promise.Promise} A promise that will resolve after
 * Definition of the state changed when the promise is resolved.
 */
function <%= name %>(options) {

  return; // return a promise
};

module.exports = <%= name %>;
